# Server Configuration
PORT=3001
NODE_ENV=development

# Supabase Configuration
SUPABASE_URL=your_supabase_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ===========================================
# NEXUS BLOCKCHAIN CONFIGURATION
# ===========================================

# Base URL for Nexus API
# Options:
#   - "mock"                        -> Development mode (no real blockchain)
#   - "http://localhost:8080"       -> Local testnet node
#   - "http://<YOUR_SERVER_IP>:8080" -> Remote testnet node
NEXUS_BASE_URL=mock

# Network mode - ONLY "testnet" is allowed for MVP
# Mainnet will be enabled only after explicit approval
NEXUS_NETWORK=testnet

# Optional: API authentication key (if apiauth=1 in node config)
NEXUS_API_KEY=

# ===========================================
# PLATFORM FEE WALLET (for sponsored fees)
# ===========================================
# These credentials allow the platform to pay transaction fees
# on behalf of users. Only use TEST COINS on testnet!
#
# To set up the platform wallet:
# 1. Create a Nexus account manually via the node CLI or API
# 2. Fund it with testnet NXS coins
# 3. Add the credentials here

NEXUS_PLATFORM_USERNAME=oria_platform
NEXUS_PLATFORM_PASSWORD=YourSecurePlatformPassword123!
NEXUS_PLATFORM_PIN=1234

# Transaction fee limits (in NXS)
# maxPerTx: Maximum fee allowed per single transaction
# dailyLimit: Maximum total fees the platform will sponsor per day
NEXUS_MAX_FEE_PER_TX=0.01
NEXUS_DAILY_FEE_LIMIT=10.0
